# Cloudflare Pages + Supabase starter (Magic link)

Tartalom:
- index.html (1 fájlos frontend)
- functions/config.js (Pages Function: /config)

Fontos:
- A Cloudflare dashboard "drag & drop" Direct Upload nem fordítja le a /functions mappát.
  Ha /functions mappát használsz (mint itt), akkor Git integrációval vagy Wrangler-rel deploy-olj.

Deploy lehetőségek:
1) Git (ajánlott kezdőként)
2) Wrangler CLI (ha nem akarsz Git-et)

